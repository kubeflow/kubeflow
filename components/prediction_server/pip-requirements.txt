# Basic ML Needs
numpy==1.12.1
pandas==0.19.2
scipy==0.19.0
scikit-learn==0.18.1
sympy==1.0
statsmodels==0.8.0

# OAuth is used for authentication.
# Don't update the version of Oauth without extra testing.
oauth2client==2.2.0

# Httplib used for http access
httplib2==0.9.2

# These packages are used by our samples, and considered generally useful.
python-dateutil==2.6.0
argparse==1.4.0
six==1.10.0

# PyYaml for faster yaml reading.
PyYAML==3.12
# Wrapt is used by sitecustomize.py to create hooks into TF.
wrapt==1.10.10
# CRCmod speeds up GCS access.
crcmod==1.7
# Python client for Google APIs
google-api-python-client==1.6.2
# Used by our logging.
python-json-logger==0.1.7
# The gcloud python library (not the tool), useful for accessing GCP.
gcloud==0.18.3
# subprocess32 is for stack traces.
subprocess32==3.2.7
# Needed to be able to build the TensorFlow .whl.
wheel==0.30.0a0

# Needed for the online prediction server, i.e. prediction_server_beta.py
# and its dependencies.
WebOb==1.7.2
Paste==2.0.3
tornado==4.5.1
grpcio==1.3.0
requests==2.13.0
webapp2==3.0.0b1

# Stackdriver logging client
google-cloud-logging==1.0.0
