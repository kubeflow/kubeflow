export const chooseMambership = (membership) => {
    if (membership === 'B') {
      return 'Bronze'

    } else if (membership === 'S') {
      return 'Silver'
    } else {
      return 'Gold'
    }
  }