apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ci-pipeline-run-persistent-volume-claim
  namespace: kubeflow-ci
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
